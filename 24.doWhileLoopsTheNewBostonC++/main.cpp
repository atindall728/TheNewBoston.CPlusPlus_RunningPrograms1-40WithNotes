#include <iostream>
#include <cmath>


using namespace std;

int main()

 // The "do while" loop is basically the "while" loop flipped upside down
 // This type of program allows for program to run through the code once before seeing out results of text
{
+
    int x = 99;

    do{

            cout << x << endl;

            x++;  // you must increment x or the test will run forever.  So Here its just going to add 1 to 99
            // *The main point of this program allows program to run at least one time before testing it.
            // People do this`
